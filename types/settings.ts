export interface Settings {
  environment: string;
  database: string;
  usernameDB: string;
  userPasswordDB: string;
  dbHost: string;
  host: string;
  serverPort: string;
  dbPort: string;
  dbURL: string;
}